import { Input } from "@/components/ui/input";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";

export type ReportFilterState = { dateFrom: string; dateTo: string; asOf: string; supplierId: string; employeeId: string; department: string; currency: string; status: string };

export function PurchaseReportFilters({ value, onChange, suppliers = [], employees = [] }: { value: ReportFilterState; onChange: (value: ReportFilterState) => void; suppliers?: Record<string, any>[]; employees?: Record<string, any>[] }) {
  const set = (key: keyof ReportFilterState, next: string) => onChange({ ...value, [key]: next });
  return <div className="grid gap-2 rounded-md border bg-card p-3 sm:grid-cols-2 lg:grid-cols-4 xl:grid-cols-8"><Input type="date" value={value.dateFrom} onChange={(event) => set("dateFrom", event.target.value)} aria-label="Date from" /><Input type="date" value={value.dateTo} onChange={(event) => set("dateTo", event.target.value)} aria-label="Date to" /><Input type="date" value={value.asOf} onChange={(event) => set("asOf", event.target.value)} aria-label="As of date" /><Select value={value.supplierId} onValueChange={(next) => set("supplierId", next)}><SelectTrigger><SelectValue placeholder="Supplier" /></SelectTrigger><SelectContent><SelectItem value="all">All suppliers</SelectItem>{suppliers.map((row) => <SelectItem key={row.id} value={row.id}>{row.name}</SelectItem>)}</SelectContent></Select><Select value={value.employeeId} onValueChange={(next) => set("employeeId", next)}><SelectTrigger><SelectValue placeholder="Employee" /></SelectTrigger><SelectContent><SelectItem value="all">All employees</SelectItem>{employees.map((row) => <SelectItem key={row.id} value={row.id}>{row.name ?? row.email ?? row.id}</SelectItem>)}</SelectContent></Select><Input placeholder="Department" value={value.department === "all" ? "" : value.department} onChange={(event) => set("department", event.target.value || "all")} /><Input placeholder="Currency" maxLength={3} value={value.currency === "all" ? "" : value.currency} onChange={(event) => set("currency", event.target.value.toUpperCase() || "all")} /><Input placeholder="Status" value={value.status === "all" ? "" : value.status} onChange={(event) => set("status", event.target.value || "all")} /></div>;
}